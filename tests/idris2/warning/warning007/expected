1/1: Building TestEnsureId (TestEnsureId.idr)
Warning: TestEnsureId.notId was explicitly annotated as reducing to the
identity function, but the compiler did not optimise it as such (transformation
pass 1/3).

Warning: TestEnsureId.notId was explicitly annotated as reducing to the
identity function, but the compiler did not optimise it as such (transformation
pass 2/3).

Warning: TestEnsureId.notId was explicitly annotated as reducing to the
identity function, but the compiler did not optimise it as such (transformation
pass 3/3).

