. ../../testutils.sh

case $(uname -s) in
    OpenBSD | FreeBSD | NetBSD)
        MAKE=gmake
        ;;

    *)
        MAKE=make
        ;;
esac

${MAKE} all > /dev/null
idris2 Struct.idr < input
${MAKE} clean > /dev/null
