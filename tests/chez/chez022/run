. ../../testutils.sh

case $(uname -s) in
    OpenBSD | FreeBSD | NetBSD)
        MAKE=gmake
        ;;

    *)
        MAKE=make
        ;;
esac

${MAKE} all > /dev/null
idris2 usealloc.idr < input
. ../../testutils.sh
${MAKE} clean > /dev/null
